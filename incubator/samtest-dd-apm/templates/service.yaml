apiVersion: v1
kind: Service
metadata:
  name: {{ include "samtest-dd-apm.fullname" . }}
  labels:
    {{- include "samtest-dd-apm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "samtest-dd-apm.selectorLabels" . | nindent 4 }}
